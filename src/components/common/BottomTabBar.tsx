const BottomTabBar = () => {
  return (
    <footer className="w-full h-16 flex items-center justify-around border-t bg-white">
      <button>홈</button>
      <button>지도</button>
      <button>기록</button>
      <button>마이페이지</button>
    </footer>
  );
};

export default BottomTabBar;
